/**
 * This file is no longer needed.
 * We now use OpenClaw's built-in pairing system via plugin SDK.
 *
 * Use these SDK functions instead:
 * - upsertChannelPairingRequest()
 * - readChannelAllowFromStore()
 * - buildPairingReply()
 */

export {};
